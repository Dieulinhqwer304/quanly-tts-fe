import { PlaceholderPage } from '../../../components/PlaceholderPage/PlaceholderPage';

export const CVList = () => {
    return <PlaceholderPage title='Danh sách đơn ứng tuyển (CV)' subTitle='Quản lý hồ sơ ứng viên nộp vào' />;
};
