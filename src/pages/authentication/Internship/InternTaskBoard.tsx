import { PlaceholderPage } from '../../../components/PlaceholderPage/PlaceholderPage';

export const InternTaskBoard = () => {
    return <PlaceholderPage title='Bảng công việc (Task Board)' subTitle='Quản lý task được giao (Kanban/List)' />;
};
